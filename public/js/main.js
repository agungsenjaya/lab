let tar = document.getElementById('liveToast');
if (tar) {
    let toast = new bootstrap.Toast(tar);
    toast.show();
}
// $('#tSuccess').toast('show');
// $('#tFailed').toast('show');
// alert('bisa');